
<?php
    // Binary, Unary, Ternary Operator 
    // 25+25 , 25++,   ?:
    // Arithmetic operators. + add,- sub ,* multi,** exponentiail, / divide ,% modulas

    //  Comparison operators.  >,<,<=,>=,===,==,=!,==!,<>(sign for not equalto in PHP),<=>(Spaceship operator )
    // here we get boolean  output

    $a=25;
    $b=5;

    echo $a==$b;// by doing you'll get only Value   //1  for false here we get empty space
    var_dump( $a==$b); // here you can see datatype and value  // bool(true)   for false here we get  bool(false)

    // Increment/Decrement operators. a++,++a,    a--,--a
?>
